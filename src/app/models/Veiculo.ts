export interface Veiculo {
    //(id, placa, chassi, renavam, modelo, marca, ano)
    id: number;
    placa: string;
    chassi: string;
    renavam: number;
    modelo: string;
    marca: string;
    ano: number;
  }